<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>edemokracia-application</artifactId>
        <groupId>party.mkkp.edemokracia</groupId>
        <version>${revision}</version>
    </parent>

    <artifactId>edemokracia-application-model</artifactId>

    <name>edemokracia transformed models</name>
    <description>edemokracia transformed models</description>

    <packaging>bundle</packaging>

    <build>
        <plugins>
            <!--
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.2.0</version>
                <executions>
                    <execution>
                        <id>copy-resources</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${basedir}/target/generated-resources/model</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.basedir}/target/generated-resources/model</directory>
                                    <filtering>true</filtering>
                                    <includes>
                                        <include>**/*.model</include>
                                        <include>**/*.yaml</include>
                                        <include>**/*.json</include>
                                        <include>**/*.changelog.xml</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            -->

            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <configuration>
                    <instructions>
                        <Judo-Models>name=${model-name};version=${model-version}</Judo-Models>
                        <Include-Resource>
                            {maven-resources},
                            /model=${basedir}/target/generated-resources/model
                        </Include-Resource>
                    </instructions>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-artifacts</id>
                        <phase>package</phase>
                        <goals>
                            <goal>attach-artifact</goal>
                        </goals>
                        <configuration>
                            <artifacts>
                                <artifact>
                                    <file>${project.basedir}/target/generated-resources/model/${model-name}-asm2jaxrsapi.jar</file>
                                    <type>jar</type>
                                    <classifier>jaxrsapi</classifier>
                                </artifact>
                                <artifact>
                                    <file>${project.basedir}/target/generated-resources/model/${model-name}-script2operation.jar</file>
                                    <type>jar</type>
                                    <classifier>script2operation</classifier>
                                </artifact>
                            </artifacts>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>
    <profiles>
        <profile>
            <id>create-models</id>
            <activation>
                <property>
                    <name>!skipModels</name>
                </property>
            </activation>

            <build>
                <plugins>
                    <plugin>
                        <groupId>hu.blackbelt.judo.tatami</groupId>
                        <artifactId>judo-tatami-workflow-maven-plugin</artifactId>
                        <version>${judo-tatami-version}</version>
                        <executions>
                            <execution>
                                <id>transform-psm-file</id>
                                <phase>generate-resources</phase>
                                <goals>
                                    <goal>default-workflow</goal>
                                </goals>
                                <configuration>
                                    <psm>mvn:${model-groupId}:${model-artifactId}:${model-version}!model/${model-name}-psm.model</psm>
                                    <modelVersion>${model-version}</modelVersion>
                                    <destination>${project.basedir}/target/generated-resources/model</destination>
                                    <dialectList>${dialectList}</dialectList>
                                    <validateModels>${validateModels}</validateModels>
                                    <ignoreAsm2Openapi>${ignoreOpenapi}</ignoreAsm2Openapi>
                                    <useCache>${useCache}</useCache>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
